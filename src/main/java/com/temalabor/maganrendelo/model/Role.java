package com.temalabor.maganrendelo.model;
/*

import javax.persistence.*;
import java.util.HashSet;
import java.util.Set;

@Entity
@Table(name = "roles")
public class Role {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private long id;

    @Column(name = "role")
    private String role;

    @OneToMany
    private Set<LoginData> users = new HashSet<LoginData>();

    public Role() {}

    public Role(String role){
        this.role=role;
    }

    public long getId() {
        return id;
    }

    public void setId(long id) {
        this.id = id;
    }

    public String getRole() {
        return role;
    }

    public void setRole(String role) {
        this.role = role;
    }

    public Set<LoginData> getUsers() {
        return users;
    }

    public void setUsers(Set<LoginData> users) {
        this.users = users;
    }

    @Override
    public String toString() {
        return "Role{id=" + id + ", role=" + role + "}";
    }
}
*/
